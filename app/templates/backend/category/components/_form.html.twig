{{ form_start(formCategory, { 'attr' : { 'class' : 'form backend category_backend' }} ) }}
    <div class="container_form">
        <div>
            <label for="category_name" class="form_label">Titre</label>
            {{ form_widget(formCategory.name, {attr : {class : 'form_input' }} ) }}
        </div>
        <small class="error_input_small">{{ form_errors(formCategory.name) }}</small>
    </div>

    <button type="submit" class="btn">Valider</button>
{{ form_end(formCategory) }}